# Scripts

Future helper scripts will live here.

Available scripts:

- `validate-config` - Check that `config.txt` can be parsed.
- `validate_config.py` - Check that `config.txt` can be parsed.
- `generate_audio.py` - Convert generated story text into MP3 files and subtitle timing.
- `build_site.py` - Build the static PWA into `public/`.
- `run_postprocess.py` - Run validation, audio generation, and site build in order.
